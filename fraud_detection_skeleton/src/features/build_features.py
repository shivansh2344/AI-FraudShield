
# Add domain-specific feature engineering here.
# Example ideas:
# - rolling stats per user_id (mean/median amount last N txns)
# - velocity features (txns per hour/day for user/device)
# - geodistance vs typical radius
# For skeleton, core features are already in dataset & pipeline.
